apiVersion: 1

policies:
  - orgId: 1
    receiver: fraud-rule-engine-webhook
    group_by:
      - grafana_folder
      - alertname
      - severity
    group_wait: 10s
    group_interval: 5m
    repeat_interval: 4h
